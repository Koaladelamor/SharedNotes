<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SharedNotes" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TextCustomStyle">
        <!-- Primary brand color. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="MainBackGroundColor">
        <!-- Primary brand color. -->
        <item name="android:background">@color/darkGrey</item>
        <item name="android:backgroundTint">@color/darkGrey</item>
    </style>

    <style name="MainBackGroundTitleColor">
        <!-- Primary brand color. -->
        <item name="android:background">@color/darkGreen</item>
        <item name="android:backgroundTint">@color/darkGreen</item>
        <item name="iconTint">@color/white</item>
        <item name="android:iconTint">@color/white</item>
    </style>

    <style name="NotesStyle">
        <!-- Primary brand color. -->
        <item name="android:backgroundTint">@color/darkGreen2</item>
    </style>

    <style name="DeleteButtonStyle">
        <!-- Primary brand color. -->
        <item name="iconTint">@color/white</item>
        <item name="android:iconTint">@color/white</item>
        <item name="android:backgroundTint">@color/darkRed</item>
    </style>

    <style name="DefaultButton">
        <!-- Primary brand color. -->
        <item name="android:backgroundTint">@color/darkGreen</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="LoginBackground">
        <!-- Primary brand color. -->
        <item name="android:background">@color/darkGreen</item>
    </style>

    <style name="LoginButtons">
        <!-- Primary brand color. -->
        <item name="android:backgroundTint">@color/lightGreen</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="LoginTitle">
        <!-- Primary brand color. -->
        <item name="android:textColor">@color/lightGreen</item>
    </style>

</resources>